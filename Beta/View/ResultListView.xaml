<UserControl x:Class="Beta.View.ResultListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <ListBox x:Name="lbResults" HorizontalContentAlignment="Stretch" Focusable="False" SelectionChanged="lbResults_OnSelectionChanged" PreviewMouseDown="lbResults_OnPreviewMouseDown" MaxHeight="300" ItemTemplate="{DynamicResource DataTemplate}">
        <ListBox.Resources>
            <DataTemplate x:Key="DataTemplate">
                <Grid HorizontalAlignment="Stretch" Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition></RowDefinition>
                        <RowDefinition Height="Auto" x:Name="SubTitleRowDefinition"></RowDefinition>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="title" DockPanel.Dock="Left" VerticalAlignment="Center" ToolTip="{Binding Title}" Text="{Binding Title}"/>
                    <TextBlock x:Name="subTitle" ToolTip="{Binding SubTitle}" Grid.Row="1" Text="{Binding SubTitle}"/>
                </Grid>
            </DataTemplate>
        </ListBox.Resources>
    </ListBox>
</UserControl>
